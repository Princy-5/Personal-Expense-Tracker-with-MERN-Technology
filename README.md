# Expense Tracker Frontend

React frontend for Expense Tracker application.

## Features
- Dashboard with financial overview
- Income/Expense management
- AI chatbot integration
- Responsive design

## Setup

1. Clone the repository
2. Install dependencies: `npm install`
3. Start development server: `npm run dev`
4. Open http://localhost:3000

## Environment Variables
Copy `.env.example` to `.env` and configure if needed.

# Expense Tracker Backend

A Node.js backend for Expense Tracker application with AI chatbot integration.

## Features
- User authentication (JWT)
- Income/Expense management
- Dashboard analytics
- AI chatbot (Gemini/OpenAI)
- MongoDB database

## Setup

1. Clone the repository
2. Install dependencies: `npm install`
3. Copy `.env.example` to `.env`
4. Configure environment variables:
   - `MONGODB_URI`: Your MongoDB connection string
   - `JWT_SECRET`: Your JWT secret key
   - `GEMINI_API_KEY` or `OPENAI_API_KEY`: AI service API key
5. Start server: `npm start`

## API Documentation
[Add your API endpoints documentation]





